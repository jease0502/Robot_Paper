# paper-comic 圖解方案 — Where the Heat Actually Is

**狀態：Step 1–4 完成，Step 5（生成點陣圖）阻擋中。**

`paper-comic` 在執行時自動偵測生圖後端（Codex 的 `imagegen`、Claude Code 的生圖 skill 等）。
本機 `~/.claude/skills/` 只有 `academic-paper`、`academic-paper-reviewer`、`academic-pipeline`、
`deep-research`、`humanizer-zh`、`paper-analyzer`、`paper-comic`、`paper-deck` 八個，**沒有任何生圖 skill**，
因此無法產生 PNG。本檔保存了完整的逐頁內容描述與結構化 prompt，裝好生圖 skill 後可直接接續 Step 5。

**替代交付（已完成）**：`fig/` 底下四張手工向量圖，可直接 `\includegraphics` 進 LaTeX。
見本檔末節對照表。

**數值來源約束**：所有數字取自 `paper-command-filtering-draft.md`（原始 md）。原始 md 沒有的數字一律標
`pending`，禁止推估。以下每張圖的標註文字都已逐一比對過原始 md。

---

## Step 2：推薦方案

建議 **5 張**，`paper-figure` 風格，繁體中文標註（術語保留英文）：

| # | 檔名 | 類型 | 為什麼需要它 |
|---|---|---|---|
| 0 | `00-cover.png` | 封面 | 一句話貢獻 + 視覺錨點：那條 3.2 N·m 的線 |
| 1 | `01-architecture.png` | 方法總覽 | policy → filter → ZOH → PD → 馬達 → 熱的完整資料流，含回授迴路與扭矩取樣點。**沒有這張，讀者無法理解「延遲為什麼會傷穩定裕度」** |
| 2 | `02-crossover.png` | 核心機制 A | 正交交越：為什麼「抖動重不重要」是關節的性質，不是 policy 的性質 |
| 3 | `03-band-budget.png` | 核心機制 B | 四頻段分解：ZOH 階梯只佔 1.6%，浪費在 5–25 Hz |
| 4 | `04-latency-backfire.png` | 關鍵結果 | 閉迴路延遲反效果，且只在速度上限現形 |

之所以是 5 張而不是更少：這篇有兩個獨立機制（交越、頻帶分解）＋一個反直覺結果，
硬塞成一張會變成資訊堆砌。也不建議更多——消融實驗與開迴路掃描用文字與表格就夠。

風格選 `paper-figure` 而非 `sketchnote`：用途是論文示意圖，需要能放進投稿版面。

---

## Step 4：逐頁內容描述

### 00 — 封面

**要表達**：這篇論文把一個大家以為已知的問題量了出來，答案跟直覺相反。

版面（16:9 橫版）：
- 中央一條垂直紅色虛線，標 `3.2 N·m`
- 線的左側：一個髖關節的簡化圖示，上方浮著高頻抖動的波形，標「漣漪主導 → 濾波有得賺」
- 線的右側：一個承重的膝關節，標「姿態主導 → 濾波幾乎無效」
- 底部橫幅：`Where the Heat Actually Is`，副標「抖動派與姿態派都對，只是在講不同關節」
- 右下角小字：`simulation only · Unitree Go1 · policy frozen`

標註文字（逐字）：`3.2 N·m` / `漣漪主導 Var(τ)` / `姿態主導 τ̄²` / `髖 1.24–1.48 N·m` / `小腿 5.88–6.88 N·m`

### 01 — 方法總覽（架構圖）

**要表達**：命令從 policy 到馬達繞了哪些關卡，本文動的是哪一關，以及回授迴路的存在。

版面（寬橫版，左到右主鏈 + 底部回授）：
- 左：`Policy π`，標 `weights frozen`、`50 Hz`
- 箭頭標 `q*ₜ`
- 虛線群組框 `command filter — 本文的介入點（policy 不重訓）`，內含三個串接方框：
  `AR(8) 預測 2 步` → `Hann kernel 對稱、零相位` → `load gate τ̄ < 3.2 N·m 才濾`
  群組框下方小字：`不付延遲：先估出對稱核所需的未來樣本，而不是等它到`
- 箭頭標 `q̃*ₜ`
- `ZOH 保持 10 ticks`，小字 `階梯 → kₚ·Δq 暫態`
- `馬達端 PD 500 Hz`，小字 `在驅動器上，不在 policy`
- `關節 + 馬達 Go1，12 DoF`
- 從關節向下拉出紅色 `τ(t)`，分兩路：
  - 右路進 `銅損代理量 H = (1/T)∫τ²dt ∝ I²ᵣₘₛR`
  - 左路進四頻段色帶（DC 31.3% / 0–5 Hz 50.6% / 5–25 Hz 16.5% / >25 Hz 1.6%），
    其中 5–25 Hz 與 >25 Hz 用括號框起標 `濾波器可觸及 = 18.1%`
  - `>25 Hz` 那格只有 9.6 px 寬——**這個視覺上的極窄就是論點本身**，必須用引線拉出標註
- 底部虛線回授箭頭從關節繞回 policy，標 `觀測回授 (50 Hz)`，下方一行：
  `policy 本身就是回授控制器 —— 在這條路徑上為了換零相位而多加的 40 ms，直接從穩定裕度扣除`

### 02 — 正交交越

**要表達**：H = τ̄² + Var(τ) 平方相加，所以哪一項主導完全由關節負載決定。

版面（左圖右註）：
- 左：折線圖，x = 關節靜態扭矩 τ̄ (0–8 N·m)，y = 佔銅損比例 (0–100%)
  - 兩條曲線交會於 x = 3.2、y = 50%，交會點畫實心圓，垂直紅虛線貫穿全圖
  - 上方標 `交越點 τ̄ = √Var(τ) = 3.2 N·m`
  - 曲線標 `漣漪 Var(τ)`（下降）與 `姿態 τ̄²`（上升）
  - 左半區標 `濾波有得賺`，右半區標 `濾波幾乎無效`
  - x 軸下方三段色塊標出 Go1 實測落點：髖 1.24–1.48、大腿 2.00–2.59、小腿 5.88–6.88
- 右欄：`實測證實了這個排序` + 三行可觸及佔比：髖 33–45%、大腿 20–32%、小腿 14–17%
  + `12 個關節中有 8 個在交越點以下，4 個膝關節在以上`
  + 結論句：`支持抖動說最有力的 G1 案例是 ankle roll —— 人形腿上靜態負載最小的關節之一`

**誠實標註（必須畫上）**：`曲線為 r = Var/(τ̄²+Var) 的定義式，非擬合資料；落點與佔比為實測值。`

### 03 — 四頻段熱預算

**要表達**：可觸及的預算有多大，以及它隨 reward 設計怎麼變。

版面（兩條上下堆疊的水平長條，同寬）：
- 上條 `full reward`，左側標 `ZOH 熱 255.9`，分段 31.3 / 50.6 / 16.5 / 1.6，括號標 `可觸及 18.1%`
- 下條 `no torques + energy`，左側標 `ZOH 熱 376.3 (+47%)`，分段 27.0 / 29.7 / 40.7 / 2.6，括號標 `可觸及 43.3%`
- 兩條之間一個虛線箭頭：`移除 effort 項 → 25 個百分點的熱搬進可觸及帶`
- 兩條的 `>25 Hz` 格都極窄，各自用引線標 `1.6%` / `2.6%`
- 底部一行：`predictive filter 實際拿回：full reward 47% of 18.1% → H/H₀ = 0.915；
  no effort terms 54% of 43.3% → H/H₀ = 0.765`
- 右下結論：`原始節省相差近三倍，回收「比例」卻穩定。量一次基線 rollout 的頻帶切分、
  把可觸及份額除以二，就能在動手前預估收益。`

標題（畫在圖上）：`大家指著的 ZOH 階梯只佔 1.6% —— 浪費其實在 5–25 Hz`

### 04 — 閉迴路延遲的反效果

**要表達**：同一個濾波器在低速是最佳、在高速會摔，而低速評估看不出來。

版面（三組長條圖，共用一條 ZOH 基線）：
- y 軸 = H / H(ZOH)，基線 1.00 畫粗虛線，標 `往上 = 更熱`
- 三組 x：`0.5 m/s`、`1.0 m/s`、`0.3 m/s + turn`
- 每組三根：`zero-phase via 40 ms delay`、`Butterworth 5 Hz`、`predictive + gate（不付延遲）`
- 數值（全部來自原始 md）：
  - 0.5 m/s：0.883 / 0.880 / 0.952，跌倒率 0.00 / 0.00 / 0.00
  - 1.0 m/s：1.258 / 1.295 / 0.924，跌倒率 0.20 / 0.70 / 0.00
  - 0.3+turn：0.992 / 0.991 / 0.956，跌倒率 0.00 / 0.00 / 0.00
- 1.0 m/s 那兩根往上竄的長條裡直接印跌倒率 `20%` / `70%`，predictive+gate 印 `0%`
- 組別下方註記：`延遲方案在這裡看起來最好` / `同一個濾波器，在這裡壞掉`
- 底部：`在無 effort 項的 policy 上，延遲方案整體跌倒率 0.37，在 1.0 m/s 是每一個回合都跌。
  最該被幫忙的 policy，被害得最慘。`
- 收尾：`只在低速評估濾波器，會把它記錄成成功，然後在部署時失敗。`

---

## Step 5：結構化 prompt（待生圖後端就緒）

四張的共用尾段：

```
【風格】paper-figure
【語言】繁體中文標註，技術術語保留英文原文
【配色限制】
- 背景：白或極淺灰
- 主體：黑／深灰線條與文字
- 強調色只用兩種：深青藍 #1F6F8B（方法／可觸及）、磚紅 #C4351F（熱／失效）
- 不要漸層、不要陰影、不要 3D
【禁止】
- 不要照片寫實、不要 3D 渲染、不要手繪塗鴉風
- 不要出現任何未列在「要標註的文字」清單裡的數字
- 不要生成使用者未確認的額外頁面
- 不要大面積空白；主體圖解佔畫面 75%–85%
```

各張的 `【類型】`／`【主題】`／`【視覺結構】`／`【要標註的文字】` 直接取用上節 Step 4 的逐頁描述。
逐字標註清單就是描述中所有以 `` ` `` 標記的字串，生成時必須逐字出現、不得改寫數字。

---

## 已交付的向量替代圖

| paper-comic 頁 | 已完成的 SVG | 說明 |
|---|---|---|
| 01 方法總覽 | `fig/fig1-architecture.svg` | 完整資料流 + 回授迴路 + 扭矩取樣點 + 四頻段色帶 |
| 02 正交交越 | `fig/fig2-crossover.svg` | 定義式曲線 + 3.2 N·m 交越 + Go1 三關節族落點 |
| 03 頻段預算 | `fig/fig3-band-budget.svg` | 雙 policy 對照 + 可觸及括號 + 回收比例 |
| 04 延遲反效果 | `fig/fig4-latency-backfire.svg` | 三速度分組 + 跌倒率內嵌 |
| 00 封面 | 未做 | 封面偏傳播用途，向量圖效益低，建議等生圖後端 |

SVG 用 `currentColor` 與 CSS 變數上色：獨立開啟時走黑線 + 預設強調色（適合投稿），
內嵌進網頁時跟著頁面主題走。
